#!/bin/bash

# hack until I can get network-gate to work in other cmdscripts
# when one command script calls another
# or when the grcyptogen main script calls a cmd script
NETWORK="testNetwork"
#exit
#
#if [ -z "$1" ]; then
#  echo "error: need to specify network as first argument"
#  exit 1
#fi
#
#NETWORK="${1}"
#
#if [ -d "../networks/${NETWORK}" ]; then
#  echo "Found network: ${NETWORK}."
#else
#  echo "Could not find network: ${NETWORK}."
#  exit 1
#fi
